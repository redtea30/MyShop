server:
  port: 8070

# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myshop
    username: root
    password: qwertyuiop

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # 配置 Nacos Server 地址
        service: shop-user # 给服务起个名字 默认值是 ${spring.application.name} 所以也
